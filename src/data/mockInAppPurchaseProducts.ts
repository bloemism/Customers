import type { InAppPurchaseProduct } from '../types/inAppPurchaseProduct';

/** カタログは Supabase `iap_catalog_items` から読み込み。ここは空のまま（旧サンプルは削除済み） */
export const mockInAppPurchaseProducts: InAppPurchaseProduct[] = [];
